import * as React from 'react';
import { Button, View, Alert, Text } from 'react-native';
const LoginPage = () => {
   return <Text>You have reached inside Login Page!</Text>;
};
export default LoginPage;